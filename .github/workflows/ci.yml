name: CI

on:
  pull_request:
  push:

permissions:
  contents: read

jobs:
  spoon-kit:
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v7.0.0

      - uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: "26.6"

      - name: Build SpoonUI
        working-directory: SpoonKit
        run: swift build

      - name: Test SpoonCore
        working-directory: SpoonKit
        run: swift test --filter SpoonCoreTests
